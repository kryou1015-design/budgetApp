interface:
  display_name: "Budget CLI TDD"
  short_description: "Build and review the CSV budget CLI with TDD and quality gates."
  default_prompt: "Use this skill when implementing or reviewing the CSV-based budget CLI app. Start with tests, keep functions small and typed, and run pytest plus radon cc."
